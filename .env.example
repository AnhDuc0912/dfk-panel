# Root directory containing projects to manage (must be absolute or relative to project)
ROOT_DIR=./repos

# Port for the web panel
PORT=3333

# Nginx configuration path and reload command (adjust for Windows if needed)
NGINX_CONF=/etc/nginx/nginx.conf
NGINX_RELOAD_CMD=nginx -s reload
